<header class="header">
    <div class="container">
        <nav class="nav">
            <div class="logo">ForumTech</div>
            <ul class="nav-links">
                <li><a href={{ path('home') }} >Accueil</a></li>
                <li><a href={{ path('app_actuality_index'') }} >Actualités</a></li>
                <li><a href={{ path('category.index') }} >Catégories</a></li>
                <li><a href={{ path('app_posts_index') }} >Sujets</a></li>
                <li><a href={{ path('app_member_index') }} >Membres</a></li>
                <li><a href={{ path('app_search_index') }} >Recherche</a></li>

            </ul>
            <div class="user-menu">
                {% if app.user %}
                    <button class="btn btn-secondary" onclick="window.location.href='{{ path('app_logout') }}'">
                        Déconnexion
                    </button>
                {% else %}
                    <button class="btn btn-secondary" onclick="showLoginModal()">Connexion</button>
                    <button class="btn btn-primary" onclick="showRegisterModal()">S'inscrire</button>
                {% endif %}

            </div>
        </nav>
    </div>

</header>

<div id="modal-container"></div>



