<header class="header">
    <div class="container">
        <nav class="nav">
            <div class="logo">
                <a href="{{ path('home') }}">ForumTech</a></div>
            <ul class="nav-links">
                <li><a href="{{ path('home') }}" >Accueil</a></li>
                <li><a href="{{ path('topic.index') }}" >Sujets</a></li>
                <li><a href="{{ path('category.index') }}" >Catégories</a></li>
            </ul>
            <div class="user-menu">
                <button class="btn btn-secondary" onclick="toggleNotifications()">🔔</button>
                <button class="btn btn-primary" onclick="showLoginModal()">Connexion</button>
            </div>
        </nav>
    </div>
</header>
